{
  "name": "webapp",
  "private": true,
  "dependencies": {

  },
  "devDependencies": {
    "gulp": "gulpjs/gulp#4.0",
    "gulp-data": ">=1",
    "gulp-swig": ">=0.8",
    "gulp-imagemin": ">= 3",
    "gulp-concat": ">=2.6",
    "gulp-less": ">=3.1",
    "gulp-load-plugins": ">=1.2",
    "gulp-cssnano": ">=2.1",
    "gulp-htmlmin": ">=2.0",
    "gulp-uglify": ">=1.5",
    "gulp-rev-all": ">=0.8",
    "gulp-babel": ">=6.1",
    "gulp-util": ">= 3",
    "gulp-changed": ">=1.3",
    "gulp-plumber": ">= 1.1",
    "gulp-task-listing": ">= 1.0.1",
    "babel-preset-es2015": ">= 6",
    "babel-preset-react": ">= 6",
    "colors": ">= 1.1",
    "del": ">=2"
  },
  "scripts": {
    "clean": "node scripts/clean.js",
    "gulp": "node node_modules/gulp/bin/gulp.js",
    "build:mall": "npm run clean && npm run gulp mall:common_js && npm run gulp build:mall",
    "build:main": "npm run clean && npm run gulp main:common_js && npm run gulp build:main"
  }
}
