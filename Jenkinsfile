pipeline {
    stages {
        stage('checkout') {
            steps {
                sh 'npm --version'
            }
        }
        stage('build') {
            steps {
                sh 'npm --version'
            }
        }
        stage('publish') {
            steps {
                sh 'npm --version'
            }
        }
    }
}